<Page
    x:Class="GetFit.Shared.Pages.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GetFit.Shared.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Light"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="10">
        <StackPanel>
            <Button HorizontalAlignment="Stretch" Margin="10" Content="Upgrade Premium" FontSize="18"></Button>
            <Button HorizontalAlignment="Stretch" Margin="10" Content="Metrics" FontSize="18"></Button>
            <Button HorizontalAlignment="Stretch" Margin="10" Content="Feedback" FontSize="18"></Button>
        </StackPanel>
    </Grid>
</Page>
