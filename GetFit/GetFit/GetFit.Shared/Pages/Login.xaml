<Page
    x:Class="GetFit.Shared.Pages.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GetFit.Shared.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Light"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="10">
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/main_background.jpg" Stretch="UniformToFill"></ImageBrush>
        </Grid.Background>
        <StackPanel Padding="10" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <TextBox x:Name="tbUsername" PlaceholderText="Username" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0 10 0 10"></TextBox>
            <PasswordBox x:Name="tbPassword" PlaceholderText="Password" HorizontalAlignment="Stretch" Margin="0 10 0 10" ></PasswordBox>
            <Button x:Name="btnLogin" Click="btnLogin_Click" Content="Login"  HorizontalAlignment="Stretch" Margin="0 10 0 10"></Button>
            <TextBlock FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10 0 10" Text="Don't Have An Account?">
                <Hyperlink Click="hyperRegister_Click">Click Here!</Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>
